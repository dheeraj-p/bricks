class Ball {
  constructor(radius, bottom, left) {
    this.radius = radius;
    this.bottom = bottom;
    this.left = left;
  }

  getRadius() {
    return this.radius;
  }

  getBottom() {
    return this.bottom;
  }

  getLeft() {
    return this.left;
  }
}
